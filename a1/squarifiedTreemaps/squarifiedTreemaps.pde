import java.util.TreeMap;
TreeMap treemap;
String lines[];
void setup(){
 lines = loadStrings("hierarchy.shf");
  int num_leaves = Integer.parseInt(lines[0]);
  print (num_leaves);
}

void draw(){
  
}
